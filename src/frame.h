#pragma once
#include "config.h"

class Frame {
    private:

    public:
        
        char call[MAX_CALLSIGN_LENGTH + 1] = {0};
    
        Frame();    // Konstruktor: Wird beim Erstellen aufgerufen

};


